<%- include('../partical/header')%>
    <!-- <h1>Order Confirmation</h1>
		<p th:text="${message}"></p>
		<h2>Thông tin khách hàng</h2>
    <div>
        <p><strong>Tên:</strong> <span th:text="${customerName}"></span></p>
        <p><strong>Email:</strong> <span th:text="${customerEmail}"></span></p>
        <p><strong>Số điện thoại:</strong> <span th:text="${customerPhone}"></span></p>
        <p><strong>Địa chỉ:</strong> <span th:text="${customerAddress}"></span></p>
        <p><strong>Trạng thái:</strong> <span th:text="${orderStatus}"></span></p>
        <p><strong>Ghi chú:</strong> <span th:text="${orderPaymentMethod}"></span></p>
    </div>
    <h1>Mặt hàng mua sắm</h1>
    <table class="table table-striped">
        <thead>
        <tr>
            <th>Tên sản phẩm</th>
            <th>Số lượng</th>
            <th>Giá tiền</th>
        </tr>
        </thead>
        <tbody>
        <tr th:each="item : ${cartItems}">
            <td th:text="${item.product.name}"></td>
            <td th:text="${item.quantity}"></td>
            <td th:text="${#numbers.formatDecimal(item.product.price * item.quantity, 1, 2)}"></td>
        </tr>
        </tbody>
    </table>
    <div class="mt-3">
        <strong>Tổng tiền thanh toán:</strong> <span th:text="${#numbers.formatDecimal(total, 1, 2)}"></span>
    </div> -->
    <div class="alert alert-success">
        Bạn đã đặt hàng thành công! Decor Store xin cảm ơn!
    </div>
    <%- include('../partical/footer')%>